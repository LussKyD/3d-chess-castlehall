// chessEngine.js — placeholder for game logic
// For the MVP this file is a stub. When you're ready, install chess.js and replace the stubbed functions
// with calls to the library: `npm install chess.js` and then `import { Chess } from 'chess.js'`
// This keeps the starter repo runnable without extra steps.

export function initialFEN(){
  // standard starting position FEN
  return 'rn1qkbnr/ppp1pppp/8/3p4/3P4/8/PPP1PPPP/RN1QKBNR b KQkq - 0 1' // placeholder (you can replace)
}

export function validateMove(/* from, to */){
  // implement using chess.js later
  return true
}
